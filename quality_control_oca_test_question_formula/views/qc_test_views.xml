<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Extend the QC Test Question form view -->
    <record id="qc_test_question_form_view_formula" model="ir.ui.view">
        <field name="name">qc.test.question.form.formula</field>
        <field name="model">qc.test.question</field>
        <field name="inherit_id" ref="quality_control_oca.qc_test_question_form_view" />
        <field name="arch" type="xml">
            <field name="type" position="after">
                <group name="formula_section" invisible="type != 'formula'">
                    <!-- Example: value >= 5 and value <= 10 -->
                    <field
                        name="formula_code"
                        widget="ace"
                        options="{'mode': 'python'}"
                        string="Formula"
                        placeholder="value &gt;= 5 and value &lt;= 10"
                        required="type == 'formula'"
                        colspan="4"
                    />
                </group>
            </field>
        </field>
    </record>
    <record id="qc_inspection_form_view_formula_fields" model="ir.ui.view">
        <field name="name">qc.inspection.form.formula.fields</field>
        <field name="model">qc.inspection</field>
        <field name="inherit_id" ref="quality_control_oca.qc_inspection_form_view" />
        <field name="arch" type="xml">
            <field name="qualitative_value" position="attributes">
                <attribute
                    name="readonly"
                >question_type in ('quantitative','formula')</attribute>
            </field>
            <field name="quantitative_value" position="attributes">
                <attribute
                    name="readonly"
                >question_type in ('qualitative','formula')</attribute>
            </field>
        </field>
    </record>
</odoo>
